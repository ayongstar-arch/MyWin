# Environment Configuration for MyWin Production

# --- Database (MySQL from Hostinger) ---
DB_HOST=your-mysql-host.hostinger.com
DB_PORT=3306
DB_USER=your_db_username
DB_PASSWORD=your_db_password
DB_NAME=mywin_db

# --- Redis (External Service Required) ---
# Option 1: Use Redis Cloud (Upstash, Redis Labs) - Recommended
# Option 2: Use in-memory caching (Limited scaling)
REDIS_URL=redis://default:password@your-redis-host:6379

# --- Security ---
JWT_SECRET=your-very-long-and-secure-secret-key-here-minimum-32-chars
NODE_ENV=production

# --- SMS Service (Production) ---
SMS_API_KEY=your-thaibulksms-api-key
SMS_API_SECRET=your-thaibulksms-secret
SMS_SENDER_ID=MyWin

# --- OAuth Providers ---
# LINE Login
LINE_CHANNEL_ID=your-line-channel-id
LINE_CHANNEL_SECRET=your-line-channel-secret
LINE_CALLBACK_URL=https://your-domain.com/api/auth/line/callback

# Google OAuth
GOOGLE_CLIENT_ID=your-google-client-id
GOOGLE_CLIENT_SECRET=your-google-client-secret
GOOGLE_CALLBACK_URL=https://your-domain.com/api/auth/google/callback

# Frontend URL (for OAuth redirects)
FRONTEND_URL=https://your-domain.com

# --- Server Settings ---
PORT=3000

# --- Development Only ---
# ALLOW_TEST_OTP=true
